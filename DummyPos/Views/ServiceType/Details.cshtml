@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
    ViewData["Title"] = "Seervice Detail";
    Layout = "_AdminLayout";
}
@model DummyPos.Models.ServiceType

<div class="container mt-4">
    <div class="card col-md-6 offset-md-3 shadow">
        <div class="card-header bg-info text-white">
            <h4>Service Details</h4>
        </div>
        <div class="card-body">
            <dl class="row mb-0">
                <dt class="col-sm-4 text-muted">Service ID:</dt>
                <dd class="col-sm-8">@Model.Service_Type_Id</dd>

                <dt class="col-sm-4 text-muted">Description:</dt>
                <dd class="col-sm-8 text-primary fw-bold">@Model.Service_Type_Desc</dd>

                <dt class="col-sm-4 text-muted">Status:</dt>
                <dd class="col-sm-8">
                    @if (Model.Is_Active)
                    {
                        <span class="badge bg-success">Active</span>
                    }
                    else
                    {

                        <span class="badge bg-danger">Inactive</span>
                    }
                </dd>

                <dt class="col-sm-4 text-muted">Created Date:</dt>
                <dd class="col-sm-8">@Model.Created_Date?.ToString("f")</dd>
            </dl>
        </div>
        <div class="card-footer bg-light">
            <a asp-action="Edit" asp-route-id="@Model.Service_Type_Id" class="btn btn-warning">Edit</a>
            <a asp-action="Index" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
</div>