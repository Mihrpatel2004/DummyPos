@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@{
    ViewData["Title"] = "Manage Tables";
    Layout = "_AdminLayout";
}
@model IEnumerable<DummyPos.Models.RestaurantTable>

<div class="card shadow p-3 mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h3>Manage Tables (Branch @ViewBag.BranchId)</h3>
        <div>
            <a asp-controller="Branch" asp-action="BranchList" class="btn btn-secondary me-2">Back to Branches</a>
            <a asp-action="Create" asp-route-branchId="@ViewBag.BranchId" class="btn btn-primary">Add New Table</a>
        </div>
    </div>

    <table class="table table-bordered table-hover text-center">
        <thead class="table-dark">
            <tr>
                <th>Table Number</th>
                <th>Location/Desc</th>
                <th>Seats</th>
                <th>Status</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                @foreach (var item in Model)
                {
                    <tr>
                        <td><strong>@item.Table_Number</strong></td>
                        <td>@item.Table_Desc</td>
                        <td><i class="fas fa-users text-muted"></i> @item.Size</td>
                        <td>
                            @if (item.Status == "Available")
                            {
                                <span class="badge bg-success">Available</span>
                            }
                            else
                            {

                                <span class="badge bg-warning text-dark">@item.Status</span>
                            }
                        </td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Table_Id" class="btn btn-outline-warning btn-sm">Edit</a>
                            <a asp-action="Delete" asp-route-id="@item.Table_Id" asp-route-branchId="@item.Branch_Id" class="btn btn-outline-danger btn-sm" onclick="return confirm('Delete this table?');">Delete</a>
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr><td colspan="5" class="text-muted py-4">No tables added to this branch yet.</td></tr>
            }
        </tbody>
    </table>
</div>